--- a/Cargo.toml
+++ b/Cargo.toml
@@ -1,153 +1,96 @@
+# Normalized from exact Reth v1.11.3 commit d6324d63e27ef6b7c49cdc9b1977c1b808234c7b.
+# Standalone normalization preserves the original git + tag dependency identity.
+
 [package]
-name = "reth-network"
-version.workspace = true
-edition.workspace = true
-rust-version.workspace = true
-license.workspace = true
-homepage.workspace = true
-repository.workspace = true
-description = "Ethereum network support"
+"name" = "reth-network"
+"version" = "1.11.3"
+"edition" = "2024"
+"rust-version" = "1.88"
+"license" = "MIT OR Apache-2.0"
+"homepage" = "https://paradigmxyz.github.io/reth"
+"repository" = "https://github.com/paradigmxyz/reth"
+"description" = "Ethereum network support"

 [lints]
-workspace = true
+"rust" = { "missing_debug_implementations" = "warn", "missing_docs" = "warn", "rust_2018_idioms" = { "level" = "deny", "priority" = -1 }, "unreachable_pub" = "warn", "unused_must_use" = "deny", "rust_2024_incompatible_pat" = "warn" }
+"rustdoc" = { "all" = "warn" }
+"clippy" = { "borrow_as_ptr" = "warn", "branches_sharing_code" = "warn", "clear_with_drain" = "warn", "cloned_instead_of_copied" = "warn", "collection_is_never_read" = "warn", "dbg_macro" = "warn", "derive_partial_eq_without_eq" = "warn", "doc_markdown" = "warn", "empty_line_after_doc_comments" = "warn", "empty_line_after_outer_attr" = "warn", "enum_glob_use" = "warn", "equatable_if_let" = "warn", "explicit_into_iter_loop" = "warn", "explicit_iter_loop" = "warn", "flat_map_option" = "warn", "from_iter_instead_of_collect" = "warn", "if_not_else" = "warn", "if_then_some_else_none" = "warn", "implicit_clone" = "warn", "imprecise_flops" = "warn", "iter_on_empty_collections" = "warn", "iter_on_single_items" = "warn", "iter_with_drain" = "warn", "iter_without_into_iter" = "warn", "large_stack_frames" = "warn", "manual_assert" = "warn", "manual_clamp" = "warn", "manual_is_variant_and" = "warn", "manual_string_new" = "warn", "match_same_arms" = "warn", "missing-const-for-fn" = "warn", "mutex_integer" = "warn", "naive_bytecount" = "warn", "needless_bitwise_bool" = "warn", "needless_continue" = "warn", "needless_for_each" = "warn", "needless_pass_by_ref_mut" = "warn", "nonstandard_macro_braces" = "warn", "option_as_ref_cloned" = "warn", "or_fun_call" = "warn", "path_buf_push_overwrite" = "warn", "read_zero_byte_vec" = "warn", "result_large_err" = "allow", "redundant_clone" = "warn", "redundant_else" = "warn", "single_char_pattern" = "warn", "string_lit_as_bytes" = "warn", "string_lit_chars_any" = "warn", "suboptimal_flops" = "warn", "suspicious_operation_groupings" = "warn", "trailing_empty_array" = "warn", "trait_duplication_in_bounds" = "warn", "transmute_undefined_repr" = "warn", "trivial_regex" = "warn", "tuple_array_conversions" = "warn", "type_repetition_in_bounds" = "warn", "uninhabited_references" = "warn", "unnecessary_self_imports" = "warn", "unnecessary_struct_initialization" = "warn", "unnested_or_patterns" = "warn", "unused_peekable" = "warn", "unused_rounding" = "warn", "use_self" = "warn", "useless_let_if_seq" = "warn", "while_float" = "warn", "zero_sized_map_values" = "warn", "as_ptr_cast_mut" = "allow", "cognitive_complexity" = "allow", "debug_assert_with_mut_call" = "allow", "fallible_impl_from" = "allow", "future_not_send" = "allow", "needless_collect" = "allow", "non_send_fields_in_send_ty" = "allow", "redundant_pub_crate" = "allow", "significant_drop_in_scrutinee" = "allow", "significant_drop_tightening" = "allow", "too_long_first_doc_paragraph" = "allow" }

 [dependencies]
-# reth
-reth-chainspec.workspace = true
-reth-evm-ethereum = { workspace = true, optional = true }
-reth-fs-util.workspace = true
-reth-primitives-traits.workspace = true
-reth-net-banlist.workspace = true
-reth-network-api.workspace = true
-reth-network-p2p.workspace = true
-reth-discv4.workspace = true
-reth-discv5.workspace = true
-reth-dns-discovery.workspace = true
-reth-ethereum-forks.workspace = true
-reth-eth-wire.workspace = true
-reth-eth-wire-types.workspace = true
-reth-ecies.workspace = true
-reth-tasks.workspace = true
-reth-transaction-pool.workspace = true
-reth-storage-api.workspace = true
-reth-tokio-util.workspace = true
-reth-consensus.workspace = true
-reth-network-peers = { workspace = true, features = ["net"] }
-reth-network-types.workspace = true
-
-# ethereum
-alloy-consensus.workspace = true
-alloy-eips.workspace = true
-alloy-primitives.workspace = true
-alloy-rlp.workspace = true
-enr = { workspace = true, features = ["serde", "rust-secp256k1"] }
-discv5.workspace = true
-
-# reth-ethereum
-reth-ethereum-primitives.workspace = true
-
-# async/futures
-futures.workspace = true
-pin-project.workspace = true
-tokio = { workspace = true, features = ["io-util", "net", "macros", "rt-multi-thread", "time"] }
-tokio-stream.workspace = true
-tokio-util = { workspace = true, features = ["codec"] }
-
-# io
-serde = { workspace = true, optional = true }
-
-# metrics
-reth-metrics = { workspace = true, features = ["common"] }
-metrics.workspace = true
-
-# misc
-auto_impl.workspace = true
-aquamarine.workspace = true
-tracing.workspace = true
-rustc-hash.workspace = true
-thiserror.workspace = true
-parking_lot.workspace = true
-rayon.workspace = true
-rand.workspace = true
-rand_08.workspace = true
-secp256k1 = { workspace = true, features = ["global-context", "std", "recovery"] }
-derive_more.workspace = true
-schnellru.workspace = true
-itertools.workspace = true
-smallvec.workspace = true
+"reth-chainspec" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-evm-ethereum" = { "default-features" = false, "optional" = true, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-fs-util" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-primitives-traits" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-net-banlist" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network-api" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network-p2p" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-discv4" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-discv5" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-dns-discovery" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-ethereum-forks" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-eth-wire" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-eth-wire-types" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-ecies" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-tasks" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-transaction-pool" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-storage-api" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-tokio-util" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-consensus" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network-peers" = { "default-features" = false, "features" = ["net"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network-types" = { "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"alloy-consensus" = { "version" = "1.6.3", "default-features" = false }
+"alloy-eips" = { "version" = "1.6.3", "default-features" = false }
+"alloy-primitives" = { "version" = "1.5.6", "default-features" = false, "features" = ["map-foldhash"] }
+"alloy-rlp" = { "version" = "0.3.13", "default-features" = false, "features" = ["core-net"] }
+"enr" = { "version" = "0.13", "default-features" = false, "features" = ["serde", "rust-secp256k1"] }
+"discv5" = { "version" = "0.10" }
+"reth-ethereum-primitives" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"futures" = { "version" = "0.3" }
+"pin-project" = { "version" = "1.0.12" }
+"tokio" = { "version" = "1.44.2", "default-features" = false, "features" = ["io-util", "net", "macros", "rt-multi-thread", "time"] }
+"tokio-stream" = { "version" = "0.1.11" }
+"tokio-util" = { "version" = "0.7.4", "features" = ["codec"] }
+"serde" = { "version" = "1.0", "default-features" = false, "optional" = true }
+"reth-metrics" = { "features" = ["common"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"metrics" = { "version" = "0.24.0" }
+"auto_impl" = { "version" = "1" }
+"aquamarine" = { "version" = "0.6" }
+"tracing" = { "version" = "0.1.0", "default-features" = false, "features" = ["attributes"] }
+"rustc-hash" = { "version" = "2.0", "default-features" = false }
+"thiserror" = { "version" = "2.0.0", "default-features" = false }
+"parking_lot" = { "version" = "0.12" }
+"rayon" = { "version" = "1.7" }
+"rand" = { "version" = "0.9" }
+"rand_08" = { "package" = "rand", "version" = "0.8" }
+"secp256k1" = { "version" = "0.30", "default-features" = false, "features" = ["global-context", "recovery", "std"] }
+"derive_more" = { "version" = "2", "default-features" = false, "features" = ["full"] }
+"schnellru" = { "version" = "0.2" }
+"itertools" = { "version" = "0.14", "default-features" = false }
+"smallvec" = { "version" = "1" }

 [dev-dependencies]
-# reth
-reth-discv4 = { workspace = true, features = ["test-utils"] }
-
-# we need to enable the test-utils feature in our own crate to use utils in
-# integration tests
-reth-network = { workspace = true, features = ["test-utils"] }
-reth-network-p2p = { workspace = true, features = ["test-utils"] }
-reth-network-types = { workspace = true, features = ["test-utils"] }
-
-reth-provider = { workspace = true, features = ["test-utils"] }
-reth-tracing.workspace = true
-reth-transaction-pool = { workspace = true, features = ["test-utils"] }
-
-# alloy deps for testing against nodes
-alloy-genesis.workspace = true
-
-# misc
-url.workspace = true
-secp256k1 = { workspace = true, features = ["rand"] }
-
-## Benchmarks
-criterion = { workspace = true, features = ["async_tokio", "html_reports"] }
+"reth-discv4" = { "features" = ["test-utils"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network" = { "features" = ["test-utils"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network-p2p" = { "features" = ["test-utils"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-network-types" = { "features" = ["test-utils"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-provider" = { "features" = ["test-utils"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-tracing" = { "default-features" = false, "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"reth-transaction-pool" = { "features" = ["test-utils"], "git" = "https://github.com/paradigmxyz/reth.git", "tag" = "v1.11.3" }
+"alloy-genesis" = { "version" = "1.6.3", "default-features" = false }
+"url" = { "version" = "2.3", "default-features" = false }
+"secp256k1" = { "version" = "0.30", "default-features" = false, "features" = ["global-context", "recovery", "rand"] }
+"criterion" = { "package" = "codspeed-criterion-compat", "version" = "4.3", "features" = ["async_tokio", "html_reports"] }

 [features]
-serde = [
-    "dep:serde",
-    "secp256k1/serde",
-    "enr/serde",
-    "reth-network-types/serde",
-    "reth-dns-discovery/serde",
-    "reth-eth-wire/serde",
-    "reth-eth-wire-types/serde",
-    "alloy-consensus/serde",
-    "alloy-eips/serde",
-    "alloy-primitives/serde",
-    "discv5/serde",
-    "parking_lot/serde",
-    "rand/serde",
-    "smallvec/serde",
-    "url/serde",
-    "reth-primitives-traits/serde",
-    "reth-ethereum-forks/serde",
-    "reth-network/serde",
-    "reth-transaction-pool/serde",
-    "reth-ethereum-primitives/serde",
-    "reth-network-api/serde",
-    "rand_08/serde",
-    "reth-storage-api/serde",
-]
-test-utils = [
-    "reth-transaction-pool/test-utils",
-    "reth-network-types/test-utils",
-    "reth-chainspec/test-utils",
-    "reth-consensus/test-utils",
-    "reth-discv4/test-utils",
-    "reth-network/test-utils",
-    "reth-network-p2p/test-utils",
-    "reth-primitives-traits/test-utils",
-    "reth-provider/test-utils",
-    "reth-ethereum-primitives/test-utils",
-    "dep:reth-evm-ethereum",
-    "reth-evm-ethereum?/test-utils",
-    "reth-tasks/test-utils",
-]
+"serde" = ["dep:serde", "secp256k1/serde", "enr/serde", "reth-network-types/serde", "reth-dns-discovery/serde", "reth-eth-wire/serde", "reth-eth-wire-types/serde", "alloy-consensus/serde", "alloy-eips/serde", "alloy-primitives/serde", "discv5/serde", "parking_lot/serde", "rand/serde", "smallvec/serde", "url/serde", "reth-primitives-traits/serde", "reth-ethereum-forks/serde", "reth-network/serde", "reth-transaction-pool/serde", "reth-ethereum-primitives/serde", "reth-network-api/serde", "rand_08/serde", "reth-storage-api/serde"]
+"test-utils" = ["reth-transaction-pool/test-utils", "reth-network-types/test-utils", "reth-chainspec/test-utils", "reth-consensus/test-utils", "reth-discv4/test-utils", "reth-network/test-utils", "reth-network-p2p/test-utils", "reth-primitives-traits/test-utils", "reth-provider/test-utils", "reth-ethereum-primitives/test-utils", "dep:reth-evm-ethereum", "reth-evm-ethereum?/test-utils", "reth-tasks/test-utils"]

 [[bench]]
-name = "broadcast"
-required-features = ["test-utils"]
-harness = false
+"name" = "broadcast"
+"required-features" = ["test-utils"]
+"harness" = false

 [[bench]]
-name = "tx_manager_hash_fetching"
-required-features = ["test-utils"]
-harness = false
+"name" = "tx_manager_hash_fetching"
+"required-features" = ["test-utils"]
+"harness" = false
--- a/src/eth_requests.rs
+++ b/src/eth_requests.rs
@@ -1,4 +1,5 @@
 //! Blocks/Headers management for the p2p network.
+// LogEx patch: preserve ETH70 progress and skip already-cancelled serving work.

 use crate::{
     budget::DEFAULT_BUDGET_TRY_DRAIN_DOWNLOADERS, metered_poll_nested_stream_with_budget,
@@ -155,6 +156,9 @@
         response: oneshot::Sender<RequestResult<BlockHeaders<C::Header>>>,
     ) {
         self.metrics.eth_headers_requests_received_total.increment(1);
+        if response.is_closed() {
+            return
+        }
         let headers = self.get_headers_response(request);
         let _ = response.send(Ok(BlockHeaders(headers)));
     }
@@ -166,6 +170,9 @@
         response: oneshot::Sender<RequestResult<BlockBodies<<C::Block as Block>::Body>>>,
     ) {
         self.metrics.eth_bodies_requests_received_total.increment(1);
+        if response.is_closed() {
+            return
+        }
         let mut bodies = Vec::new();

         let mut total_bytes = 0;
@@ -194,6 +201,9 @@
         response: oneshot::Sender<RequestResult<Receipts<C::Receipt>>>,
     ) {
         self.metrics.eth_receipts_requests_received_total.increment(1);
+        if response.is_closed() {
+            return
+        }

         let receipts = self.get_receipts_response(request, |receipts_by_block| {
             receipts_by_block.into_iter().map(ReceiptWithBloom::from).collect::<Vec<_>>()
@@ -209,6 +219,9 @@
         response: oneshot::Sender<RequestResult<Receipts69<C::Receipt>>>,
     ) {
         self.metrics.eth_receipts_requests_received_total.increment(1);
+        if response.is_closed() {
+            return
+        }

         let receipts = self.get_receipts_response(request, |receipts_by_block| {
             // skip bloom filter for eth69
@@ -220,7 +233,8 @@

     /// Handles partial responses for [`GetReceipts70`] queries.
     ///
-    /// This will adhere to the soft limit but allow filling the last vec partially.
+    /// Targets the soft limit, permitting one oversized receipt to make progress.
+    /// The final block may be filled partially when more receipts remain.
     fn on_receipts70_request(
         &self,
         _peer_id: PeerId,
@@ -228,6 +242,9 @@
         response: oneshot::Sender<RequestResult<Receipts70<C::Receipt>>>,
     ) {
         self.metrics.eth_receipts_requests_received_total.increment(1);
+        if response.is_closed() {
+            return
+        }

         let GetReceipts70 { first_block_receipt_index, block_hashes } = request;

@@ -263,18 +280,25 @@
                 continue;
             }

+            let remaining_count = block_receipts.len();
             let mut partial_block = Vec::new();
             for receipt in block_receipts {
                 let receipt_size = receipt.length();
-                if total_bytes + receipt_size > SOFT_RESPONSE_LIMIT {
+                // The target is soft: permit one large receipt only when the
+                // response would otherwise make no block or receipt progress.
+                if total_bytes.saturating_add(receipt_size) > SOFT_RESPONSE_LIMIT &&
+                    (!receipts.is_empty() || !partial_block.is_empty())
+                {
                     break;
                 }
-                total_bytes += receipt_size;
+                total_bytes = total_bytes.saturating_add(receipt_size);
                 partial_block.push(receipt);
             }

-            receipts.push(partial_block);
-            last_block_incomplete = true;
+            if !partial_block.is_empty() {
+                last_block_incomplete = partial_block.len() < remaining_count;
+                receipts.push(partial_block);
+            }
             break;
         }

--- a/src/session/active.rs
+++ b/src/session/active.rs
@@ -1,4 +1,5 @@
 //! Represents an established session.
+// LogEx patch: announce coherent changed ranges on the existing epoch interval.

 use core::sync::atomic::Ordering;
 use std::{
@@ -16,6 +17,7 @@
     session::{
         conn::EthRlpxConnection,
         handle::{ActiveSessionMessage, SessionCommand},
+        range_update::should_send_range_update,
         BlockRangeInfo, EthVersion, SessionId,
     },
 };
@@ -26,7 +28,8 @@
 use reth_eth_wire::{
     errors::{EthHandshakeError, EthStreamError},
     message::{EthBroadcastMessage, MessageError},
-    Capabilities, DisconnectP2P, DisconnectReason, EthMessage, NetworkPrimitives, NewBlockPayload,
+    BlockRangeUpdate, Capabilities, DisconnectP2P, DisconnectReason, EthMessage, NetworkPrimitives,
+    NewBlockPayload,
 };
 use reth_eth_wire_types::{message::RequestPair, RawCapabilityMessage};
 use reth_metrics::common::mpsc::MeteredPollSender;
@@ -47,8 +50,8 @@
 /// The recommended interval at which to check if a new range update should be sent to the remote
 /// peer.
 ///
-/// Updates are only sent when the block height has advanced by at least one epoch (32 blocks)
-/// since the last update. The interval is set to one epoch duration in seconds.
+/// Changed ranges are announced at most once per epoch duration. This includes
+/// history pruning, reorgs and regressions, not just forward head progress.
 pub(super) const RANGE_UPDATE_INTERVAL: Duration = Duration::from_secs(EPOCH_SLOTS * 12);

 // Constants for timeout updating.
@@ -129,12 +132,10 @@
     /// This represents the range of blocks that this node can serve to other peers.
     pub(crate) local_range_info: BlockRangeInfo,
     /// Optional interval for sending periodic range updates to the remote peer (eth69+)
-    /// The interval is set to one epoch duration (~6.4 minutes), but updates are only sent when
-    /// the block height has advanced by at least one epoch (32 blocks) since the last update
+    /// The interval is set to one epoch duration (~6.4 minutes).
     pub(crate) range_update_interval: Option<Interval>,
-    /// The last latest block number we sent in a range update
-    /// Used to avoid sending unnecessary updates when block height hasn't changed significantly
-    pub(crate) last_sent_latest_block: Option<u64>,
+    /// The exact snapshot last queued for announcement; unchanged tuples are suppressed.
+    pub(crate) last_sent_range: Option<BlockRangeUpdate>,
 }

 impl<N: NetworkPrimitives> ActiveSession<N> {
@@ -741,21 +742,14 @@
         }

         if let Some(interval) = &mut this.range_update_interval {
-            // Check if we should send a range update based on block height changes
             while interval.poll_tick(cx).is_ready() {
-                let current_latest = this.local_range_info.latest();
-                let should_send = if let Some(last_sent) = this.last_sent_latest_block {
-                    // Only send if block height has advanced by at least one epoch (32 blocks)
-                    current_latest.saturating_sub(last_sent) >= EPOCH_SLOTS
-                } else {
-                    true // First update, always send
-                };
-
-                if should_send {
-                    this.queued_outgoing.push_back(
-                        EthMessage::BlockRangeUpdate(this.local_range_info.to_message()).into(),
-                    );
-                    this.last_sent_latest_block = Some(current_latest);
+                let current = this.local_range_info.to_message();
+                if should_send_range_update(this.last_sent_range.as_ref(), &current) {
+                    this.queued_outgoing
+                        .push_back(EthMessage::BlockRangeUpdate(current.clone()).into());
+                    this.last_sent_range = Some(current);
+                    // This queue is drained earlier in poll; schedule its flush.
+                    cx.waker().wake_by_ref();
                 }
             }
         }
@@ -1078,7 +1072,7 @@
                             alloy_primitives::B256::ZERO,
                         ),
                         range_update_interval: None,
-                        last_sent_latest_block: None,
+                        last_sent_range: None,
                     }
                 }
                 ev => {
--- a/src/session/mod.rs
+++ b/src/session/mod.rs
@@ -1,9 +1,11 @@
 //! Support for handling peer sessions.
+// LogEx patch: remember the complete last announced range.

 mod active;
 mod conn;
 mod counter;
 mod handle;
+mod range_update;
 mod types;
 pub use types::BlockRangeInfo;

@@ -574,7 +576,7 @@
                     range_info: None,
                     local_range_info: self.local_range_info.clone(),
                     range_update_interval,
-                    last_sent_latest_block: None,
+                    last_sent_range: None,
                 };

                 self.spawn(session);
--- a/src/session/types.rs
+++ b/src/session/types.rs
@@ -1,36 +1,24 @@
 //! Shared types for network sessions.
+// LogEx patch: publish and read complete range tuples under one lock.

 use alloy_primitives::B256;
 use parking_lot::RwLock;
 use reth_eth_wire::BlockRangeUpdate;
-use std::{
-    ops::RangeInclusive,
-    sync::{
-        atomic::{AtomicU64, Ordering},
-        Arc,
-    },
-};
+use std::{ops::RangeInclusive, sync::Arc};

 /// Information about the range of full blocks available from a peer.
 ///
-/// This represents the announced `eth69`
-/// [`BlockRangeUpdate`] of a peer.
+/// This represents the announced `eth69` [`BlockRangeUpdate`] of a peer.
 #[derive(Debug, Clone)]
 pub struct BlockRangeInfo {
-    /// The inner range information.
-    inner: Arc<BlockRangeInfoInner>,
+    /// Range numbers and hash belong to the same publication.
+    inner: Arc<RwLock<BlockRangeUpdate>>,
 }

 impl BlockRangeInfo {
     /// Creates a new range information.
     pub fn new(earliest: u64, latest: u64, latest_hash: B256) -> Self {
-        Self {
-            inner: Arc::new(BlockRangeInfoInner {
-                earliest: AtomicU64::new(earliest),
-                latest: AtomicU64::new(latest),
-                latest_hash: RwLock::new(latest_hash),
-            }),
-        }
+        Self { inner: Arc::new(RwLock::new(BlockRangeUpdate { earliest, latest, latest_hash })) }
     }

     /// Returns true if the block number is within the range of blocks available from the peer.
@@ -40,24 +28,23 @@

     /// Returns the range of blocks available from the peer.
     pub fn range(&self) -> RangeInclusive<u64> {
-        let earliest = self.earliest();
-        let latest = self.latest();
-        RangeInclusive::new(earliest, latest)
+        let snapshot = self.inner.read();
+        snapshot.earliest..=snapshot.latest
     }

     /// Returns the earliest full block number available from the peer.
     pub fn earliest(&self) -> u64 {
-        self.inner.earliest.load(Ordering::Relaxed)
+        self.inner.read().earliest
     }

     /// Returns the latest full block number available from the peer.
     pub fn latest(&self) -> u64 {
-        self.inner.latest.load(Ordering::Relaxed)
+        self.inner.read().latest
     }

     /// Returns the latest block hash available from the peer.
     pub fn latest_hash(&self) -> B256 {
-        *self.inner.latest_hash.read()
+        self.inner.read().latest_hash
     }

     /// Returns true if the peer has the full history available.
@@ -65,30 +52,13 @@
         self.earliest() == 0
     }

-    /// Updates the range information.
+    /// Updates the range information as one publication.
     pub fn update(&self, earliest: u64, latest: u64, latest_hash: B256) {
-        self.inner.earliest.store(earliest, Ordering::Relaxed);
-        self.inner.latest.store(latest, Ordering::Relaxed);
-        *self.inner.latest_hash.write() = latest_hash;
+        *self.inner.write() = BlockRangeUpdate { earliest, latest, latest_hash };
     }

-    /// Converts the current range information to an Eth69 [`BlockRangeUpdate`] message.
+    /// Returns a coherent Eth69 [`BlockRangeUpdate`] snapshot.
     pub fn to_message(&self) -> BlockRangeUpdate {
-        BlockRangeUpdate {
-            earliest: self.earliest(),
-            latest: self.latest(),
-            latest_hash: self.latest_hash(),
-        }
+        self.inner.read().clone()
     }
 }
-
-/// Inner structure containing the range information with atomic and thread-safe fields.
-#[derive(Debug)]
-pub(crate) struct BlockRangeInfoInner {
-    /// The earliest block which is available.
-    earliest: AtomicU64,
-    /// The latest block which is available.
-    latest: AtomicU64,
-    /// Latest available block's hash.
-    latest_hash: RwLock<B256>,
-}
--- /dev/null
+++ b/src/session/range_update.rs
@@ -0,0 +1,56 @@
+//! Range advertisement change detection, called only on the existing epoch tick.
+use reth_eth_wire::BlockRangeUpdate;
+
+pub(super) fn should_send_range_update(
+    last_sent: Option<&BlockRangeUpdate>,
+    current: &BlockRangeUpdate,
+) -> bool {
+    last_sent != Some(current)
+}
+
+#[cfg(test)]
+mod tests {
+    use alloy_primitives::B256;
+
+    use super::*;
+
+    fn range(earliest: u64, latest: u64, hash: u8) -> BlockRangeUpdate {
+        BlockRangeUpdate { earliest, latest, latest_hash: B256::repeat_byte(hash) }
+    }
+
+    #[test]
+    fn range_update_first_and_unchanged_tuple() {
+        let current = range(50, 100, 1);
+        assert!(should_send_range_update(None, &current));
+        assert!(!should_send_range_update(Some(&current), &current));
+    }
+
+    #[test]
+    fn range_update_full_epoch_forward_progress() {
+        assert!(should_send_range_update(Some(&range(50, 100, 1)), &range(50, 132, 2)));
+    }
+
+    #[test]
+    fn range_update_earliest_changes_without_head_progress() {
+        let previous = range(50, 100, 1);
+        assert!(should_send_range_update(Some(&previous), &range(60, 100, 1)));
+        assert!(should_send_range_update(Some(&previous), &range(40, 100, 1)));
+    }
+
+    #[test]
+    fn range_update_same_height_reorg() {
+        assert!(should_send_range_update(Some(&range(50, 100, 1)), &range(50, 100, 2)));
+    }
+
+    #[test]
+    fn range_update_regression_and_genesis_fallback() {
+        let previous = range(50, 100, 1);
+        assert!(should_send_range_update(Some(&previous), &range(50, 90, 2)));
+        assert!(should_send_range_update(Some(&previous), &range(0, 0, 3)));
+    }
+
+    #[test]
+    fn range_update_sub_epoch_change_is_not_indefinitely_hidden() {
+        assert!(should_send_range_update(Some(&range(50, 100, 1)), &range(50, 101, 2)));
+    }
+}
